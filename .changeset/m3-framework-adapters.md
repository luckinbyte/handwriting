---
'@handwriting/react': minor
'@handwriting/wc': minor
'@handwriting/core': patch
---

M3: framework adapters — `@handwriting/react` (`useHandwritingEngine` hook) and
`@handwriting/wc` (`<handwriting-stream>` custom element), plus a three-page demo
(vanilla / React / Web Component) and core SSR no-op guard tests.
